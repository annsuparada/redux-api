
const innitialState = {
    user: [],
    posts: [],
    isLoading: false,
    error: ''
}

export const reducer = (state = innitialState, action) => {
    switch (action.type) {

        default:
            return state
    }
}